# Core Flask server
flask==2.3.3
flask-cors==4.0.0
werkzeug==2.3.7

# Image processing
Pillow==10.3.0
numpy==1.26.4

# Stage 1 — MobileNetV2 bird verifier (ONNX Runtime)
onnxruntime==1.18.0

# Stage 2 — 525-species fine-grained bird classifier (HuggingFace)
transformers==5.13.1
torch==2.5.0
soxr==1.1.0
