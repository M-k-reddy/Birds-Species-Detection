# Core Flask server
flask==2.3.3
flask-cors==4.0.0
werkzeug==2.3.7

# Image processing
Pillow==10.3.0
numpy==1.26.4

# ONNX Runtime (Stage 1 - MobileNetV2 bird verifier)
onnxruntime==1.18.0

# Hugging Face (Stage 2 - 525 species fine-grained classifier)
transformers==4.41.2
torch==2.3.0
torchvision==0.18.0

# Optional: Google Cloud Vision (only needed if using Vision API)
# google-cloud-vision==3.4.2
